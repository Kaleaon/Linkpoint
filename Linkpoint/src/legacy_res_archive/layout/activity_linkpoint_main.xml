<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/linkpoint_background">

    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/linkpointToolbar"
        style="@style/Widget.MaterialComponents.Toolbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:elevation="4dp"
        android:background="@color/linkpoint_surface"
        android:titleTextAppearance="@style/TextAppearance.MaterialComponents.Headline6"
        android:title="@string/linkpoint_world_title" />

    <FrameLayout
        android:id="@+id/contentContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginBottom="72dp"
        android:layout_marginTop="?attr/actionBarSize"
        app:layout_behavior="@string/appbar_scrolling_view_behavior" />

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottomNavigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:background="@color/linkpoint_surface"
        app:itemIconTint="@color/selector_linkpoint_nav"
        app:itemRippleColor="@color/linkpoint_primary_variant"
        app:itemTextColor="@color/selector_linkpoint_nav"
        app:labelVisibilityMode="labeled"
        app:menu="@menu/menu_linkpoint_main" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
