<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.linkpoint">

    <!-- Network permissions -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE"/>
    
    <!-- WebRTC Voice Chat Permissions -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS"/>
    <uses-permission android:name="android.permission.BLUETOOTH"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"/>
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" android:minSdkVersion="31"/>
    
    <!-- Storage permissions (for caching textures, etc.) -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"/>
    
    <!-- Wake lock for voice chat -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    
    <!-- Camera (optional, for future video features) -->
    <uses-permission android:name="android.permission.CAMERA" android:required="false"/>
    
    <!-- Hardware features -->
    <uses-feature android:name="android.hardware.microphone" android:required="false"/>
    <uses-feature android:name="android.hardware.camera" android:required="false"/>
    <uses-feature android:glEsVersion="0x00030002" android:required="true"/>

    <application
        android:name=".core.LinkpointApplication"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_linkpoint_launcher"
        android:roundIcon="@mipmap/ic_linkpoint_launcher_round"
        android:theme="@style/Theme.Linkpoint"
        android:allowBackup="true"
        android:supportsRtl="true"
        android:hardwareAccelerated="true"
        android:largeHeap="true">

        <!-- Main Activity -->
        <activity
            android:name=".ui.LinkpointMainActivity"
            android:label="@string/app_name"
            android:exported="true"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- World View Activity (immersive 3D view) -->
        <activity
            android:name=".ui.LinkpointWorldActivity"
            android:label="@string/world_view_title"
            android:theme="@style/Theme.Linkpoint.NoActionBar"
            android:screenOrientation="landscape"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:exported="false"/>

        <!-- Settings Activity -->
        <activity
            android:name=".ui.LinkpointSettingsActivity"
            android:label="@string/settings_title"
            android:parentActivityName=".ui.LinkpointMainActivity"
            android:exported="false"/>
            
        <!-- Second Life SLURL handler -->
        <activity
            android:name=".ui.LinkpointSLURLActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.Linkpoint"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="secondlife"/>
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="http"/>
                <data android:host="maps.secondlife.com"/>
                <data android:pathPrefix="/secondlife"/>
            </intent-filter>
        </activity>

    </application>
</manifest>