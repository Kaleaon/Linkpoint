package com.linkpoint.react

/**
 * Interface for objects that can request a refresh without parameters.
 * Converted from Java to Kotlin for better type safety.
 */
interface RefreshableOne {
    fun requestRefresh()
}