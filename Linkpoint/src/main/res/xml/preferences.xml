<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- Display Settings -->
    <PreferenceCategory android:title="Display">

        <ListPreference
            android:key="screen_orientation"
            android:title="Screen Orientation"
            android:summary="Choose how the world view is displayed"
            android:entries="@array/screen_orientation_entries"
            android:entryValues="@array/screen_orientation_values"
            android:defaultValue="portrait" />

    </PreferenceCategory>

    <!-- Interface Settings -->
    <PreferenceCategory android:title="Interface">

        <SwitchPreferenceCompat
            android:key="show_hud"
            android:title="Show HUD"
            android:summary="Display in-world HUD overlays"
            android:defaultValue="true" />

        <SwitchPreferenceCompat
            android:key="show_joysticks"
            android:title="Show Joysticks"
            android:summary="Display movement and camera joysticks"
            android:defaultValue="true" />

        <SwitchPreferenceCompat
            android:key="show_action_buttons"
            android:title="Show Action Buttons"
            android:summary="Display gesture and social buttons"
            android:defaultValue="true" />

        <SwitchPreferenceCompat
            android:key="show_movement_buttons"
            android:title="Show Movement Buttons"
            android:summary="Display fly, run, jump, and sit buttons"
            android:defaultValue="true" />

        <Preference
            android:key="customize_layout"
            android:title="Customize Layout"
            android:summary="Reposition on-screen controls" />

        <Preference
            android:key="open_ktheme_theme_menu"
            android:title="Ktheme Themes"
            android:summary="Choose from built-in and shared Ktheme themes" />

    </PreferenceCategory>

    <!-- Graphics Settings -->
    <PreferenceCategory android:title="Graphics">

        <ListPreference
            android:key="graphics_quality"
            android:title="Graphics Quality"
            android:summary="Overall rendering quality"
            android:entries="@array/graphics_quality_entries"
            android:entryValues="@array/graphics_quality_values"
            android:defaultValue="medium" />

        <SeekBarPreference
            android:key="draw_distance"
            android:title="Draw Distance"
            android:summary="How far to render objects"
            android:min="32"
            android:max="512"
            android:defaultValue="128"
            app:showSeekBarValue="true" />

        <SwitchPreferenceCompat
            android:key="enable_shadows"
            android:title="Shadows"
            android:summary="Enable dynamic shadows"
            android:defaultValue="true" />

        <SwitchPreferenceCompat
            android:key="enable_water_reflections"
            android:title="Water Reflections"
            android:summary="Enable water reflections"
            android:defaultValue="true" />

        <SwitchPreferenceCompat
            android:key="enable_secondary_renderer"
            android:title="Use Secondary Renderer (Lumiya)"
            android:summary="Switch to the alternate OpenGL renderer (requires world view restart)"
            android:defaultValue="false" />

    </PreferenceCategory>

    <!-- Camera Settings -->
    <PreferenceCategory android:title="Camera">

        <SeekBarPreference
            android:key="camera_fov"
            android:title="Field of View"
            android:summary="Vertical field of view in degrees (default 60)"
            android:min="45"
            android:max="90"
            android:defaultValue="60"
            app:showSeekBarValue="true" />

        <SeekBarPreference
            android:key="camera_follow_distance"
            android:title="Follow Distance"
            android:summary="Distance behind the avatar in follow mode (meters)"
            android:min="1"
            android:max="30"
            android:defaultValue="5"
            app:showSeekBarValue="true" />

        <SeekBarPreference
            android:key="camera_orbit_sensitivity"
            android:title="Orbit Sensitivity"
            android:summary="How fast the camera rotates per drag pixel (×0.05 deg/px)"
            android:min="1"
            android:max="20"
            android:defaultValue="5"
            app:showSeekBarValue="true" />

        <SwitchPreferenceCompat
            android:key="camera_invert_y"
            android:title="Invert Y Axis"
            android:summary="Flip vertical drag direction"
            android:defaultValue="false" />

    </PreferenceCategory>

    <!-- Audio Settings -->
    <PreferenceCategory android:title="Audio">

        <SeekBarPreference
            android:key="master_volume"
            android:title="Master Volume"
            android:min="0"
            android:max="100"
            android:defaultValue="75"
            app:showSeekBarValue="true" />

        <SwitchPreferenceCompat
            android:key="enable_voice"
            android:title="Voice Chat"
            android:summary="Enable voice chat"
            android:defaultValue="true" />

        <SwitchPreferenceCompat
            android:key="enable_streaming"
            android:title="Streaming Audio"
            android:summary="Play parcel audio streams"
            android:defaultValue="true" />

    </PreferenceCategory>

    <!-- XR Settings -->
    <PreferenceCategory android:title="XR / VR">

        <SwitchPreferenceCompat
            android:key="enable_xr"
            android:title="Enable XR Mode"
            android:summary="Requires Android 15+ immersive XR hardware or Cardboard-compatible VR mode"
            android:defaultValue="true" />

        <ListPreference
            android:key="xr_mode"
            android:title="XR Mode"
            android:summary="Auto selects supported runtime; experimental runtimes require xrExperimental build"
            android:entries="@array/xr_mode_entries"
            android:entryValues="@array/xr_mode_values"
            android:defaultValue="auto" />

        <SeekBarPreference
            android:key="xr_movement_speed"
            android:title="Movement Speed"
            android:summary="XR locomotion speed"
            android:min="1"
            android:max="10"
            android:defaultValue="5"
            app:showSeekBarValue="true" />

    </PreferenceCategory>

    <!-- Cache Settings - Lumiya-style configurable caches -->
    <PreferenceCategory android:title="Cache &amp; Memory">

        <ListPreference
            android:key="cache_location"
            android:title="Cache Location"
            android:summary="Where to store cached assets"
            android:entries="@array/cache_location_entries"
            android:entryValues="@array/cache_location_values"
            android:defaultValue="documents" />

        <EditTextPreference
            android:key="custom_cache_path"
            android:title="Custom Cache Path"
            android:summary="Custom location for Lumiya Cache folder"
            android:defaultValue=""
            android:inputType="text" />

        <Preference
            android:key="cache_path_info"
            android:title="Current Cache Path"
            android:summary="Tap to view full path" />

        <SeekBarPreference
            android:key="disk_cache_size"
            android:title="Total Disk Cache (GB)"
            android:summary="Maximum disk space for all cached assets (up to 50 GB)"
            android:min="1"
            android:max="50"
            android:defaultValue="4"
            app:showSeekBarValue="true" />

        <SeekBarPreference
            android:key="memory_cache_size"
            android:title="Memory Cache (MB)"
            android:summary="RAM for general asset caching (100-4096 MB)"
            android:min="100"
            android:max="4096"
            android:defaultValue="512"
            app:showSeekBarValue="true" />

        <SeekBarPreference
            android:key="texture_memory"
            android:title="Texture Memory (MB)"
            android:summary="RAM dedicated to decoded textures (64-2048 MB)"
            android:min="64"
            android:max="2048"
            android:defaultValue="512"
            app:showSeekBarValue="true" />

        <SeekBarPreference
            android:key="texture_cache_size"
            android:title="Texture Cache (GB)"
            android:summary="Disk space for texture cache (up to 20 GB)"
            android:min="1"
            android:max="20"
            android:defaultValue="2"
            app:showSeekBarValue="true" />

        <SeekBarPreference
            android:key="mesh_cache_size"
            android:title="Mesh Cache (GB)"
            android:summary="Disk space for 3D model cache (up to 10 GB)"
            android:min="1"
            android:max="10"
            android:defaultValue="1"
            app:showSeekBarValue="true" />

        <SeekBarPreference
            android:key="sound_cache_size"
            android:title="Sound Cache (GB)"
            android:summary="Disk space for audio cache (up to 4 GB)"
            android:min="1"
            android:max="4"
            android:defaultValue="1"
            app:showSeekBarValue="true" />

        <SeekBarPreference
            android:key="animation_cache_size"
            android:title="Animation Cache (GB)"
            android:summary="Disk space for animation cache (up to 4 GB)"
            android:min="1"
            android:max="4"
            android:defaultValue="1"
            app:showSeekBarValue="true" />

        <SwitchPreferenceCompat
            android:key="auto_clear_cache"
            android:title="Auto-Clear on Low Space"
            android:summary="Automatically clear cache when storage is low"
            android:defaultValue="true" />

        <Preference
            android:key="view_cache_stats"
            android:title="View Cache Statistics"
            android:summary="See current cache usage" />

        <Preference
            android:key="clear_public_cache"
            android:title="Clear Public Cache"
            android:summary="Delete cached textures, meshes, sounds and animations (shared)" />

        <Preference
            android:key="clear_private_cache"
            android:title="Clear Private Cache"
            android:summary="Delete cached messages, friends lists (user-specific)" />

        <Preference
            android:key="clear_cache"
            android:title="Clear All Cache"
            android:summary="Delete all cached data (public and private)" />

    </PreferenceCategory>

    <!-- Background Runtime -->
    <PreferenceCategory android:title="Background Runtime">

        <SwitchPreferenceCompat
            android:key="background_resume_enabled"
            android:title="Resume in Background"
            android:summary="Resume connection orchestration after reboot and process death"
            android:defaultValue="true" />

        <SwitchPreferenceCompat
            android:key="push_wakeups_enabled"
            android:title="Push Wakeups"
            android:summary="Allow FCM/self-hosted relay wakeups for IM and group notices"
            android:defaultValue="true" />

        <ListPreference
            android:key="background_intensity_profile"
            android:title="Background Intensity"
            android:summary="Balance between delivery latency and battery usage"
            android:entries="@array/background_profile_entries"
            android:entryValues="@array/background_profile_values"
            android:defaultValue="balanced" />

    </PreferenceCategory>

    <!-- Network Settings -->
    <PreferenceCategory android:title="Network">

        <SeekBarPreference
            android:key="bandwidth_limit"
            android:title="Bandwidth Limit"
            android:summary="Maximum bandwidth (0 = unlimited)"
            android:min="0"
            android:max="10000"
            android:defaultValue="0"
            app:showSeekBarValue="true" />

        <SeekBarPreference
            android:key="xml_buffer_size"
            android:title="Login Buffer Size (MB)"
            android:summary="Buffer size for login/XML responses (3-500 MB)"
            android:min="3"
            android:max="500"
            android:defaultValue="100"
            app:showSeekBarValue="true" />

    </PreferenceCategory>

    <!-- RLV Settings -->
    <PreferenceCategory android:title="RLV (Restrained Love Viewer)">

        <SwitchPreferenceCompat
            android:key="rlv_enabled"
            android:title="Enable RLV"
            android:summary="Enable Restrained Love Viewer functions"
            android:defaultValue="false" />

    </PreferenceCategory>

    <!-- Debug and Diagnostics -->
    <PreferenceCategory android:title="Debug &amp; Diagnostics">

        <SwitchPreferenceCompat
            android:key="enable_debug_floater"
            android:title="Debug Report Floater"
            android:summary="Show floating bug icon to capture debug reports"
            android:defaultValue="false" />

        <SwitchPreferenceCompat
            android:key="codex_auto_upload"
            android:title="Auto-upload Debug Logs to Codex"
            android:summary="Automatically send debug reports and crash logs for review"
            android:defaultValue="false" />

        <EditTextPreference
            android:key="codex_upload_endpoint"
            android:title="Codex Upload Endpoint"
            android:summary="Optional custom endpoint for Codex uploads"
            android:defaultValue=""
            android:inputType="textUri" />

        <Preference
            android:key="codex_upload_now"
            android:title="Upload Pending Codex Reports"
            android:summary="Send any queued reports now" />

        <Preference
            android:key="capture_debug_report"
            android:title="Capture Debug Report Now"
            android:summary="Capture current app state for debugging" />

        <Preference
            android:key="force_appearance_refresh"
            android:title="Force Refresh Appearance"
            android:summary="Re-bake all 11 channels and resend AgentSetAppearance to the simulator (debug)" />

        <Preference
            android:key="view_debug_reports"
            android:title="View Debug Reports"
            android:summary="View saved debug reports" />

        <Preference
            android:key="view_crash_logs"
            android:title="View Crash Logs"
            android:summary="View saved crash reports for debugging" />

        <Preference
            android:key="crash_reporter_status"
            android:title="Crash Reporter Status"
            android:summary="Check if crash reporting is working" />

        <Preference
            android:key="test_crash"
            android:title="Test Crash Reporter"
            android:summary="Generate a test crash report (non-fatal)" />

        <Preference
            android:key="clear_crash_logs"
            android:title="Clear Crash Logs"
            android:summary="Delete all saved crash reports" />


        <SwitchPreferenceCompat
            android:key="diagnostic_verbose_packet_logging"
            android:title="Verbose Packet Logging"
            android:summary="Include packet hex dumps in private diagnostics logs"
            android:defaultValue="false" />

        <SwitchPreferenceCompat
            android:key="diagnostic_verbose_body_logging"
            android:title="Verbose HTTP Body Logging"
            android:summary="Include HTTP response body snippets in diagnostics logs"
            android:defaultValue="false" />

        <ListPreference
            android:key="diagnostic_log_retention_days"
            android:title="Diagnostics Log Retention"
            android:summary="Automatically purge private diagnostics logs older than selected days"
            android:entries="@array/diagnostic_retention_entries"
            android:entryValues="@array/diagnostic_retention_values"
            android:defaultValue="7" />

        <!--
            Combined Log = most recent debug report + crash logs + a
            header. The raw file lives in app-private storage which the
            user can't browse, so every action below moves it off-device:
            Copy → clipboard, Export → user-chosen file via SAF,
            Share → ACTION_SEND through FileProvider.
        -->
        <Preference
            android:key="copy_log"
            android:title="Copy Combined Log to Clipboard"
            android:summary="Copies debug report + crash logs as text" />

        <Preference
            android:key="export_log"
            android:title="Export Combined Log to File"
            android:summary="Save to a user-chosen location (Downloads, Drive, etc.)" />

        <Preference
            android:key="share_log"
            android:title="Share Combined Log…"
            android:summary="Send via email, messaging, or another app" />

        <!--
            Session Log Recorder.
            The recorder is auto-started at app launch (LinkpointApp.onCreate)
            and captures every packet, HTTP request, capability event, and
            render-pipeline lifecycle event into a per-session text file.
            That file is in app-private storage too, so use Copy / Export /
            Share to actually get it off the device.
        -->
        <Preference
            android:key="session_log_status"
            android:title="Session Log Recorder"
            android:summary="Recording status, file size, and entry count" />

        <Preference
            android:key="session_log_copy"
            android:title="Copy Session Log to Clipboard"
            android:summary="Copy the current session log file contents" />

        <Preference
            android:key="session_log_export"
            android:title="Export Session Log to File"
            android:summary="Save the raw session log to a user-chosen location" />

        <Preference
            android:key="session_log_share"
            android:title="Share Session Log…"
            android:summary="Share the session log via another app" />

    </PreferenceCategory>

    <!-- About -->
    <PreferenceCategory android:title="About">

        <Preference
            android:key="about"
            android:title="About Linkpoint"
            android:summary="Version information and disclaimers" />

        <Preference
            android:key="view_tos"
            android:title="Terms of Service"
            android:summary="View the Terms of Service" />

        <Preference
            android:key="privacy_policy"
            android:title="Privacy Policy"
            android:summary="View the Privacy Policy" />

        <Preference
            android:key="tpv_compliance"
            android:title="TPV Policy Compliance"
            android:summary="Third-Party Viewer Policy compliance" />

        <Preference
            android:key="version"
            android:title="Version"
            android:summary="1.0.0" />

        <Preference
            android:key="licenses"
            android:title="Open Source Licenses" />

    </PreferenceCategory>

</PreferenceScreen>
