<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    android:versionCode="67"
    android:versionName="3.4.3"
    platformBuildVersionCode="35"
    platformBuildVersionName="15">

    <!-- IMPORTANT: SDK levels are now controlled by Gradle (build.gradle). -->
    <!-- Removed <uses-sdk> from manifest to avoid conflicts with AGP 8.x. -->

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <!-- WebRTC Voice Chat Permissions -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.BLUETOOTH"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"/>
    <uses-permission android:name="android.permission.CAMERA" android:required="false"/>

    <!-- Additional permissions for advanced WebRTC features -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE"/>

    <uses-feature android:name="android.hardware.screen.landscape" android:required="false"/>
    <uses-feature android:name="android.software.vr.mode" android:required="false"/>
    <uses-feature android:name="android.hardware.vr.high_performance" android:required="false"/>
    <uses-feature android:name="android.hardware.microphone" android:required="false"/>

    <application
        android:theme="@style/Theme.Lumiya"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_lumiya_launcher"
        android:name="com.lumiyaviewer.lumiya.stub.StubLumiyaApp"
        android:allowBackup="true"
        android:installLocation="auto"
        android:supportsRtl="true"
        android:fullBackupContent="@xml/backup_rules">

        <activity
            android:label="@string/app_name"
            android:name="com.lumiyaviewer.lumiya.stub.StubCleanLoginActivity"
            android:clearTaskOnLaunch="true"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        
        <!-- All other activities disabled until Kotlin migration errors are fixed -->
        <!-- See docs/LINKPOINT_APK_BUILD_ANALYSIS.md for details on the 221,328 compilation errors -->
        
    </application>
</manifest>
