# Linkpoint Builds

This folder contains supporting build notes and artifacts for native app workflows.

## Primary Build Target

The project is focused on the native Android app.

### Build Debug APK

```bash
./gradlew assembleDebug
```

### Run Unit Tests

```bash
./gradlew test
```

## Notes

Legacy web/PWA build content was removed from this repository to keep development centered on Android.
